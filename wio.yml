type: pkg

project:
    name: uWebSockets
    version: 0.0.1
    keywords:
        - wio
        - pkg
    compile_options:
        wio_version: 0.4.0
        default_target: tests
        flags: [-std=c++11 -O3 -I src -shared -fPIC -s -o libuWS.so]

targets:
    tests:
        platform: native
        src: tests

